
server:
  port: 8085
spring:
  cache:
    type: redis
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/shortenurl
    username: postgres
    password: admin